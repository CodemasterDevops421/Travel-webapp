---
phase: 04-checkout-and-booking-lifecycle-integrity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260225_booking_lifecycle_guards.sql
  - src/server/booking/lifecycle.ts
  - src/server/booking/repository.ts
  - tests/booking-lifecycle.test.ts
  - tests/booking-repository.test.ts
autonomous: true
requirements:
  - BOOK-03
  - BOOK-05
must_haves:
  truths:
    - "Booking records only use valid lifecycle states: pending, payment_authorized, confirmed, failed, refunded."
    - "Invalid state jumps are rejected and do not mutate booking records."
    - "Booking records persist payment/confirmation/commercial fields needed for lifecycle reconciliation."
  artifacts:
    - path: "src/server/booking/lifecycle.ts"
      provides: "Central lifecycle transition map and guard helpers"
    - path: "src/server/booking/repository.ts"
      provides: "Lifecycle-aware booking update and persistence contract"
    - path: "supabase/migrations/20260225_booking_lifecycle_guards.sql"
      provides: "Additive DB enum/check/unique/trigger backstops for lifecycle integrity"
  key_links:
    - from: "src/server/booking/lifecycle.ts"
      to: "src/server/booking/repository.ts"
      via: "Repository calls lifecycle guard before persisting status transitions"
      pattern: "assertValidBookingTransition|canTransitionBookingState"
    - from: "src/server/booking/repository.ts"
      to: "bookings"
      via: "Writes canonical booking/payment fields with allowed states"
      pattern: "from\('bookings'\)"
---

<objective>
Establish explicit booking lifecycle invariants at application and database boundaries before Stripe/webhook integration work.

Purpose: Prevent invalid lifecycle drift and ensure canonical booking records can safely support payment reconciliation and side effects.
Output: Lifecycle guard module, additive migration backstops, and repository wiring/tests that enforce allowed transitions.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-checkout-and-booking-lifecycle-integrity/04-RESEARCH.md
@.planning/phases/01-platform-foundation-and-security/01-02-SUMMARY.md
@src/server/booking/repository.ts
@supabase/migrations/006_phase1_foundation.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lifecycle state machine module with transition guards</name>
  <files>src/server/booking/lifecycle.ts, tests/booking-lifecycle.test.ts</files>
  <action>Create a dedicated lifecycle module exporting the canonical states (`pending`, `payment_authorized`, `confirmed`, `failed`, `refunded`), allowed transition map, and helper functions to validate transitions. Add focused tests that cover allowed transitions, forbidden transitions, and terminal-state behavior. Avoid route-local if/else transition logic so all callers share one contract.</action>
  <verify>npm run test -- tests/booking-lifecycle.test.ts</verify>
  <done>Lifecycle helpers return true only for legal transitions and tests fail if any illegal jump is accepted.</done>
</task>

<task type="auto">
  <name>Task 2: Add additive database lifecycle integrity backstops</name>
  <files>supabase/migrations/20260225_booking_lifecycle_guards.sql</files>
  <action>Create an additive/idempotent migration that constrains booking lifecycle values to the canonical set and adds DB-level guardrails for transition integrity and idempotency-critical identifiers needed later (Stripe payment/session IDs and existing LiteAPI IDs). Keep migration fail-closed and additive only; do not rewrite or drop existing tables.</action>
  <verify>npx supabase db lint</verify>
  <done>Migration is additive, repeat-safe, and defines DB constraints/guards that reject non-canonical lifecycle values.</done>
</task>

<task type="auto">
  <name>Task 3: Wire repository writes to lifecycle guard contract</name>
  <files>src/server/booking/repository.ts, tests/booking-repository.test.ts</files>
  <action>Refactor booking persistence/update paths to call lifecycle validation before state updates, and ensure canonical fields (`liteapi_booking_id`, `confirmation_code`, `payment_status`, `total_amount`, `commission_amount`) remain populated consistently when transitions occur. Add/extend repository tests for rejected illegal transitions and accepted valid transitions.</action>
  <verify>npm run test -- tests/booking-repository.test.ts</verify>
  <done>Repository update APIs reject invalid lifecycle transitions and preserve required canonical booking fields on valid transitions.</done>
</task>

</tasks>

<verification>
Run `npm run test -- tests/booking-lifecycle.test.ts tests/booking-repository.test.ts` and confirm migration file is additive/idempotent-safe with no destructive DDL.
</verification>

<success_criteria>
BOOK-03 lifecycle transitions are explicitly enforced in code and backed by database constraints, and BOOK-05 canonical booking fields remain first-class in repository writes/updates.
</success_criteria>

<output>
After completion, create `.planning/phases/04-checkout-and-booking-lifecycle-integrity/04-01-SUMMARY.md`
</output>
