---
phase: 04-checkout-and-booking-lifecycle-integrity
plan: "03"
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/features/booking/components/booking-console.tsx
  - src/app/booking/return/booking-return-client.tsx
  - src/app/api/booking/status/route.ts
  - src/server/booking/repository.ts
  - src/server/booking-store.ts
  - tests/booking-checkout-flow.test.tsx
autonomous: true
requirements:
  - BOOK-01
must_haves:
  truths:
    - "Traveler experiences a clear 3-step checkout: guest details, payment, confirmation."
    - "Checkout progress survives refresh/redirect and resumes without losing booking context."
    - "Confirmation details render only after lifecycle-confirmed booking status is available."
  artifacts:
    - path: "src/features/booking/components/booking-console.tsx"
      provides: "3-step checkout UI and persisted progress orchestration"
    - path: "src/app/booking/return/booking-return-client.tsx"
      provides: "Post-payment status page that waits for authoritative lifecycle outcome"
    - path: "src/app/api/booking/status/route.ts"
      provides: "Polling/read endpoint for checkout return state"
  key_links:
    - from: "src/features/booking/components/booking-console.tsx"
      to: "src/server/booking-store.ts"
      via: "Persisted checkout context keyed by transaction/session"
      pattern: "booking:checkout"
    - from: "src/app/booking/return/booking-return-client.tsx"
      to: "/api/booking/status"
      via: "Poll/read lifecycle state before rendering confirmation"
      pattern: "fetch\('/api/booking/status"
---

<objective>
Deliver a resilient 3-step traveler checkout flow that persists progress and shows confirmation only from authoritative booking lifecycle state.

Purpose: Meet BOOK-01 while preventing false confirmations caused by redirect timing or local client assumptions.
Output: Persisted checkout UX, lifecycle-aware return page, and status API for deterministic confirmation rendering.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-checkout-and-booking-lifecycle-integrity/04-RESEARCH.md
@.planning/phases/03-hotel-detail-and-user-workspace/03-02-SUMMARY.md
@src/features/booking/components/booking-console.tsx
@src/app/booking/return/booking-return-client.tsx
@src/server/booking-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist and recover 3-step checkout progress</name>
  <files>src/features/booking/components/booking-console.tsx, src/server/booking-store.ts</files>
  <action>Refine booking console state so each step has explicit persisted progress (guest details captured, payment initiated, awaiting confirmation) and can recover after reload or redirect using stable transaction/session keys. Keep existing selected-rate handoff contract from Phase 3 and avoid breaking current promo/quote signature behavior.</action>
  <verify>npm run test -- tests/booking-store.test.ts</verify>
  <done>Traveler can resume checkout after refresh/redirect without re-entering already completed step data.</done>
</task>

<task type="auto">
  <name>Task 2: Add lifecycle-backed booking status read endpoint</name>
  <files>src/app/api/booking/status/route.ts, src/server/booking/repository.ts</files>
  <action>Create a secure read endpoint returning booking lifecycle/payment/confirmation snapshot for checkout return state, keyed by validated booking or transaction identifiers. Response must distinguish pending-processing vs terminal-success/failure outcomes and never infer success from client return alone.</action>
  <verify>npm run typecheck</verify>
  <done>Status endpoint returns authoritative lifecycle-driven state shape usable by the return page without side effects.</done>
</task>

<task type="auto">
  <name>Task 3: Gate return-page confirmation on authoritative status</name>
  <files>src/app/booking/return/booking-return-client.tsx, tests/booking-checkout-flow.test.tsx</files>
  <action>Update return page to poll the new status endpoint and render a processing state until lifecycle reaches a valid confirmation or failure terminal state; then route/show confirmation details accordingly. Add tests proving no confirmation UI appears while lifecycle is still pending.</action>
  <verify>npm run test -- tests/booking-checkout-flow.test.tsx</verify>
  <done>Return page only shows confirmation details when booking lifecycle/payment state is truly confirmed and otherwise shows pending or failure messaging.</done>
</task>

</tasks>

<verification>
Run `npm run test -- tests/booking-checkout-flow.test.tsx tests/booking-store.test.ts` and manually verify `/booking` -> payment redirect -> `/booking/return` shows processing until authoritative status changes.
</verification>

<success_criteria>
BOOK-01 is satisfied with persisted 3-step checkout flow and confirmation rendering strictly tied to authoritative booking lifecycle status.
</success_criteria>

<output>
After completion, create `.planning/phases/04-checkout-and-booking-lifecycle-integrity/04-03-SUMMARY.md`
</output>
