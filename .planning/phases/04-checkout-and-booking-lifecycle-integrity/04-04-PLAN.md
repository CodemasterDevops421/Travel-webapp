---
phase: 04-checkout-and-booking-lifecycle-integrity
plan: "04"
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/server/booking/outbox.ts
  - src/server/notifications/email.ts
  - src/emails/booking-confirmation.tsx
  - src/emails/booking-cancellation.tsx
  - src/app/api/bookings/[bookingId]/cancel/route.ts
  - src/server/booking/repository.ts
  - tests/booking-notification-lifecycle.test.ts
autonomous: true
requirements:
  - BOOK-06
must_haves:
  truths:
    - "Confirmation and cancellation emails reflect real booking lifecycle outcomes, not client assumptions."
    - "Cancellation/refund actions move booking and invoice state together without divergence."
    - "Duplicate event processing does not send duplicate lifecycle emails."
  artifacts:
    - path: "src/server/booking/outbox.ts"
      provides: "Idempotent side-effect dispatch from lifecycle transitions"
    - path: "src/server/notifications/email.ts"
      provides: "Lifecycle-aware email delivery adapter"
    - path: "src/app/api/bookings/[bookingId]/cancel/route.ts"
      provides: "Cancellation/refund initiation and lifecycle-safe state updates"
  key_links:
    - from: "src/server/booking/outbox.ts"
      to: "src/server/notifications/email.ts"
      via: "Dispatches transition-derived confirmation/cancellation events"
      pattern: "confirmed|refunded|failed"
    - from: "src/app/api/bookings/[bookingId]/cancel/route.ts"
      to: "src/server/booking/repository.ts"
      via: "Updates booking lifecycle and invoice status atomically"
      pattern: "invoice_status|refunded|failed"
user_setup:
  - service: resend
    why: "Deliver HTML booking confirmation/cancellation emails"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys"
      - name: BOOKING_FROM_EMAIL
        source: "Verified sender identity configured in Resend"
    dashboard_config:
      - task: "Verify sending domain/sender identity"
        location: "Resend Dashboard -> Domains / Senders"
---

<objective>
Implement lifecycle-driven communications and cancellation/refund handling so email and invoice state always match authoritative booking/payment outcomes.

Purpose: Fulfill BOOK-06 with retry-safe side effects and prevent traveler-facing messaging drift from real booking lifecycle state.
Output: Outbox-based lifecycle side-effect pipeline, HTML email templates, and cancellation endpoint updating booking + invoice state together.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-checkout-and-booking-lifecycle-integrity/04-RESEARCH.md
@src/server/booking/repository.ts
@src/server/webhook-idempotency.ts
@src/app/api/booking/book/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lifecycle-driven outbox and notification adapter</name>
  <files>src/server/booking/outbox.ts, src/server/notifications/email.ts, src/server/booking/repository.ts</files>
  <action>Create an outbox-style side-effect layer that emits events only when lifecycle transitions reach communication-relevant states (`confirmed`, `failed`, `refunded`) and dedupes dispatch per booking+transition. Wire repository transition updates to enqueue side effects without blocking main request/webhook acknowledgment paths.</action>
  <verify>npm run typecheck</verify>
  <done>Lifecycle transitions enqueue one idempotent side-effect record per booking transition and do not block booking/payment writes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement booking confirmation and cancellation HTML emails</name>
  <files>src/emails/booking-confirmation.tsx, src/emails/booking-cancellation.tsx, src/server/notifications/email.ts</files>
  <action>Create React Email templates for confirmation and cancellation/refund events and send via provider adapter with deterministic payload contracts (booking reference, dates, totals, cancellation outcome). Ensure send behavior is idempotent-safe and driven only from outbox transition events.</action>
  <verify>npm run typecheck</verify>
  <done>Confirmed/refunded lifecycle transitions can render/send deterministic HTML emails without duplicate sends on replay.</done>
</task>

<task type="auto">
  <name>Task 3: Add cancellation/refund endpoint with invoice-state synchronization</name>
  <files>src/app/api/bookings/[bookingId]/cancel/route.ts, src/server/booking/repository.ts, tests/booking-notification-lifecycle.test.ts</files>
  <action>Implement booking cancellation boundary that validates cancel/refund eligibility, calls payment refund path where required, and updates booking lifecycle plus invoice status in one consistent transition flow. Add tests asserting cancellation/refund updates trigger matching outbox/email behavior and do not diverge invoice state.</action>
  <verify>npm run test -- tests/booking-notification-lifecycle.test.ts</verify>
  <done>Cancellation/refund operations produce aligned booking lifecycle + invoice state updates and corresponding lifecycle emails exactly once.</done>
</task>

</tasks>

<verification>
Run `npm run test -- tests/booking-notification-lifecycle.test.ts` and confirm transition-triggered email dispatch logs align with booking lifecycle/invoice updates.
</verification>

<success_criteria>
BOOK-06 is satisfied: confirmation/cancellation communications and invoice state changes are transition-driven, idempotent, and aligned with authoritative booking/payment lifecycle outcomes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-checkout-and-booking-lifecycle-integrity/04-04-SUMMARY.md`
</output>
