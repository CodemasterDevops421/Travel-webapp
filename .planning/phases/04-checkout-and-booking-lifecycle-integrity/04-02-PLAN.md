---
phase: 04-checkout-and-booking-lifecycle-integrity
plan: "02"
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/server/payments/stripe.ts
  - src/app/api/checkout/session/route.ts
  - src/app/api/webhooks/stripe/route.ts
  - src/app/api/booking/book/route.ts
  - src/server/webhook-idempotency.ts
  - src/server/booking/repository.ts
  - tests/stripe-webhook-route.test.ts
  - tests/booking-finalize-idempotency.test.ts
autonomous: true
requirements:
  - BOOK-02
  - BOOK-04
  - BOOK-05
user_setup:
  - service: stripe
    why: "Checkout session creation and verified webhook authority"
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe Dashboard -> Developers -> API keys"
      - name: STRIPE_WEBHOOK_SECRET
        source: "Stripe Dashboard -> Developers -> Webhooks endpoint secret"
      - name: NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY
        source: "Stripe Dashboard -> Developers -> API keys"
    dashboard_config:
      - task: "Create webhook endpoint for /api/webhooks/stripe with payment and refund events"
        location: "Stripe Dashboard -> Developers -> Webhooks"
must_haves:
  truths:
    - "Payment state changes are accepted only from verified Stripe webhook events."
    - "Duplicate submit/retry/replayed webhook events do not produce duplicate booking outcomes."
    - "Booking records persist canonical payment and confirmation fields without conflicting duplicates."
  artifacts:
    - path: "src/app/api/webhooks/stripe/route.ts"
      provides: "Stripe signature verification, event filtering, and idempotent reconciliation"
    - path: "src/app/api/checkout/session/route.ts"
      provides: "Stripe checkout/session intent creation boundary"
    - path: "src/app/api/booking/book/route.ts"
      provides: "Finalize endpoint that no longer treats client return as payment authority"
  key_links:
    - from: "src/app/api/webhooks/stripe/route.ts"
      to: "src/server/booking/repository.ts"
      via: "Verified event-driven status/field updates"
      pattern: "constructEvent|updateBooking"
    - from: "src/app/api/booking/book/route.ts"
      to: "src/server/payments/stripe.ts"
      via: "Creates/links payment session but defers authority to webhook"
      pattern: "idempotencyKey|checkout\.sessions|paymentIntents"
---

<objective>
Make Stripe webhook events the single payment source of truth while preserving idempotent booking finalization under retries and concurrency.

Purpose: Align booking/payment correctness with BOOK-02 and BOOK-04 so no client path can falsely confirm payment or create duplicate outcomes.
Output: Stripe checkout + webhook endpoints, idempotent finalize hardening, and regression tests for replay/retry safety.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-checkout-and-booking-lifecycle-integrity/04-RESEARCH.md
@src/app/api/booking/book/route.ts
@src/app/api/webhooks/liteapi/route.ts
@src/server/booking-idempotency.ts
@src/server/webhook-idempotency.ts
@src/server/booking/repository.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Stripe payment client and checkout session boundary</name>
  <files>src/server/payments/stripe.ts, src/app/api/checkout/session/route.ts</files>
  <action>Create a server Stripe client wrapper and a checkout/session route that accepts validated booking-payment payload, creates Stripe checkout/payment session with deterministic idempotency key, and persists linkage identifiers needed for reconciliation. Keep this boundary initiation-only; do not mark bookings paid from this route.</action>
  <verify>npm run typecheck</verify>
  <done>Checkout/session route returns Stripe session identifiers and booking linkage metadata without mutating authoritative payment status.</done>
</task>

<task type="auto">
  <name>Task 2: Implement verified Stripe webhook reconciliation with event dedupe</name>
  <files>src/app/api/webhooks/stripe/route.ts, src/server/webhook-idempotency.ts, src/server/booking/repository.ts, tests/stripe-webhook-route.test.ts</files>
  <action>Implement raw-body Stripe signature verification using official SDK `constructEvent`, handle only required payment/refund events, dedupe by Stripe event id using existing webhook idempotency primitives, and update booking lifecycle/payment fields through repository transition APIs. Acknowledge quickly and defer non-critical side effects. Avoid custom signature parsing and avoid status writes from unverified payloads.</action>
  <verify>npm run test -- tests/stripe-webhook-route.test.ts</verify>
  <done>Invalid signatures are rejected, duplicate event IDs are ignored safely, and verified events produce one consistent booking lifecycle/payment update.</done>
</task>

<task type="auto">
  <name>Task 3: Harden finalize endpoint for webhook-authoritative payment truth</name>
  <files>src/app/api/booking/book/route.ts, src/server/booking/repository.ts, tests/booking-finalize-idempotency.test.ts</files>
  <action>Refactor finalize behavior so it records booking attempt/linkage data and idempotent lock/cache outcomes but does not transition to payment-authorized/confirmed without verified webhook evidence. Preserve existing lock-based retry protection and add regression tests for duplicate submits/concurrency/race scenarios.</action>
  <verify>npm run test -- tests/booking-finalize-idempotency.test.ts</verify>
  <done>Finalize retries return deterministic idempotent responses and payment-authoritative lifecycle transitions happen only after verified webhook processing.</done>
</task>

</tasks>

<verification>
Run `npm run test -- tests/stripe-webhook-route.test.ts tests/booking-finalize-idempotency.test.ts` and use Stripe CLI replay to confirm duplicate events do not create duplicate transitions.
</verification>

<success_criteria>
BOOK-02 and BOOK-04 pass with verified webhook authority and layered idempotency, and BOOK-05 booking payment linkage fields remain reconcilable and non-duplicated.
</success_criteria>

<output>
After completion, create `.planning/phases/04-checkout-and-booking-lifecycle-integrity/04-02-SUMMARY.md`
</output>
