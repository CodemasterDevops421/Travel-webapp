---
phase: 02.3-homepage-navigation-and-detail-shell-regression-closure
plan: "01"
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/components/home/featured-deals-strip.tsx
  - src/components/home/travel-articles.tsx
  - src/components/home/trending-destinations.tsx
  - tests/homepage-card-navigation.test.ts
  - tests/homepage-card-link-behavior.test.tsx
autonomous: true
requirements:
  - DISC-01
  - DISC-06
must_haves:
  truths:
    - "Homepage discovery cards are restored as real links rather than inert clickable-looking containers."
    - "Per-card navigation keeps the prior PreferenceLink contract so stored language and currency preferences survive navigation."
    - "Mouse and keyboard users can activate homepage cards without relying on section-level fallback links."
    - "Representative homepage card coverage explicitly protects focusability and tap-safe link-wrapper structure."
    - "Regression coverage locks both the source contract and one representative rendered link behavior."
  artifacts:
    - path: "src/components/home/featured-deals-strip.tsx"
      provides: "Featured deals cards wrapped in preference-aware per-card navigation."
    - path: "src/components/home/travel-articles.tsx"
      provides: "Travel journal cards restored as actual card links instead of inert articles."
    - path: "src/components/home/trending-destinations.tsx"
      provides: "Trending destination cards restored as focusable navigation targets."
    - path: "tests/homepage-card-navigation.test.ts"
      provides: "Source-contract guardrails for PreferenceLink usage and exact per-card href restoration."
    - path: "tests/homepage-card-link-behavior.test.tsx"
      provides: "Focused rendered proof that at least one homepage card remains keyboard-focusable and preference-aware."
  key_links:
    - from: "src/components/home/featured-deals-strip.tsx"
      to: "src/components/navigation/preference-link.tsx"
      via: "Featured deals must route through PreferenceLink to preserve preference params."
      pattern: "PreferenceLink|/search?q=|encodeURIComponent"
    - from: "src/components/home/travel-articles.tsx"
      to: "src/components/navigation/preference-link.tsx"
      via: "Travel journal cards must restore the same per-card preference-aware navigation contract."
      pattern: "PreferenceLink|/search?q=|focus-visible"
    - from: "src/components/home/trending-destinations.tsx"
      to: "src/components/navigation/preference-link.tsx"
      via: "Trending destination cards must be restored as links, not cursor-only cards."
      pattern: "PreferenceLink|cursor-pointer|href"
---

<objective>
Restore homepage discovery-card navigation that regressed during the recent homepage polish work.

Purpose: make featured deals, travel journal cards, and trending destinations function again as real preference-aware navigation targets for mouse and keyboard users.
Output: restored per-card `PreferenceLink` wrappers plus narrow regression tests that fail if the cards become inert again.
</objective>

<execution_context>
@C:/Users/Lenovo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.3-homepage-navigation-and-detail-shell-regression-closure/02.3-CONTEXT.md
@.planning/phases/02.3-homepage-navigation-and-detail-shell-regression-closure/02.3-RESEARCH.md
@.planning/phases/02.3-homepage-navigation-and-detail-shell-regression-closure/02.3-VALIDATION.md
@src/components/navigation/preference-link.tsx
@src/components/home/featured-deals-strip.tsx
@src/components/home/travel-articles.tsx
@src/components/home/trending-destinations.tsx
@tests/search-results-card-behavior.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore per-card PreferenceLink navigation on homepage discovery modules</name>
  <files>src/components/home/featured-deals-strip.tsx, src/components/home/travel-articles.tsx, src/components/home/trending-destinations.tsx</files>
  <action>Rewrap each homepage card with `PreferenceLink` using the prior per-card destination expressions described in the research artifact. Preserve the existing card visuals, hover styling, and section-level links, but remove the inert article-only interaction pattern so each card is a true link and remains keyboard reachable.</action>
  <verify>npm run test -- tests/homepage-card-navigation.test.ts</verify>
  <done>Homepage discovery cards once again navigate through real preference-aware links instead of cursor-only articles.</done>
</task>

<task type="auto">
  <name>Task 2: Add homepage regression guardrails for source contract and rendered behavior</name>
  <files>tests/homepage-card-navigation.test.ts, tests/homepage-card-link-behavior.test.tsx</files>
  <action>Add a focused source-contract suite that asserts `PreferenceLink` imports, per-card wrappers, focus-visible or tap-safe wrapper classes, and the restored href expressions for all three homepage modules. Add one small rendered interaction test for a representative module proving the card is exposed as a link, remains keyboard-focusable, and keeps preference params attached when store state is present.</action>
  <verify>npm run test -- tests/homepage-card-navigation.test.ts tests/homepage-card-link-behavior.test.tsx</verify>
  <done>The homepage regression cannot silently return without failing either source-contract or rendered navigation coverage.</done>
</task>

</tasks>

<verification>
Run `npm run test -- tests/homepage-card-navigation.test.ts tests/homepage-card-link-behavior.test.tsx tests/search-results-card-behavior.test.tsx` and manually confirm on `/` that featured deals, travel journal cards, and trending destinations are tabbable and activate on Enter.
</verification>

<success_criteria>
Homepage cards are restored as real `PreferenceLink` destinations with preserved preference propagation, and focused regression coverage protects both source structure and representative link behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02.3-homepage-navigation-and-detail-shell-regression-closure/02.3-01-SUMMARY.md`
</output>
