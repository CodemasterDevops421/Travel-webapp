---
phase: 02-search-and-discovery-experience
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/app/api/property-preview/route.ts
  - src/server/liteapi.ts
  - src/server/cache.ts
  - src/shared/lib/cache-ttl.ts
  - tests/search-degraded-state.test.ts
  - tests/cache-ttl.test.ts
autonomous: true
requirements:
  - DISC-05
must_haves:
  truths:
    - "Supplier-backed search responses are served through TTL caching within a 5-15 minute window"
    - "When supplier data is degraded, responses explicitly disclose degraded status and reason"
    - "Outages return truthful fallback behavior instead of silent stale data"
  artifacts:
    - path: "src/app/api/property-preview/route.ts"
      provides: "Search response envelope with degraded/freshness metadata"
    - path: "src/shared/lib/cache-ttl.ts"
      provides: "Canonical 300-900 second discovery TTL definitions"
    - path: "tests/search-degraded-state.test.ts"
      provides: "Regression tests for degraded response taxonomy"
  key_links:
    - from: "src/app/api/property-preview/route.ts"
      to: "src/server/liteapi.ts"
      via: "supplier fetch result mapped into degraded envelope"
      pattern: "degraded|reason|asOf"
    - from: "src/server/cache.ts"
      to: "src/shared/lib/cache-ttl.ts"
      via: "cache TTL values consumed by provider cache wrappers"
      pattern: "revalidate|TTL"
---

<objective>
Implement cache-backed supplier search behavior with explicit degraded-state truthfulness.

Purpose: keep discovery fast under normal conditions while maintaining user trust during upstream instability.
Output: 5-15 minute TTL search caching and a typed degraded response contract for supplier failures.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-search-and-discovery-experience/02-RESEARCH.md
@.planning/phases/02-search-and-discovery-experience/02-01-SUMMARY.md
@src/app/api/property-preview/route.ts
@src/server/liteapi.ts
@src/shared/lib/cache-ttl.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define discovery caching TTLs and response envelope contract</name>
  <files>src/shared/lib/cache-ttl.ts, src/server/cache.ts, src/app/api/property-preview/route.ts</files>
  <action>Expand search TTL constants so supplier-backed discovery endpoints run inside the 300-900 second target range, and shape API responses into an envelope that includes data freshness and degradation metadata (`degraded`, `degradedReason`, `asOf`). Keep contract additive to avoid breaking existing consumers unexpectedly.</action>
  <verify>npm run lint && npm run typecheck</verify>
  <done>Property-preview responses expose explicit freshness/degraded metadata and cache durations are aligned to 5-15 minute policy.</done>
</task>

<task type="auto">
  <name>Task 2: Implement truthful degraded-state mapping for supplier failure classes</name>
  <files>src/server/liteapi.ts, src/app/api/property-preview/route.ts</files>
  <action>Classify supplier fetch failures into a small degraded taxonomy (for example timeout, partial, unavailable) and propagate that status through API responses with safe fallback payload behavior. Avoid silent fallback paths that appear fresh when upstream failures occurred.</action>
  <verify>npm run test -- tests/search-degraded-state.test.ts</verify>
  <done>Timeout/partial/unavailable supplier states are reflected in API payload metadata and corresponding fallback behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for TTL and degraded response correctness</name>
  <files>tests/search-degraded-state.test.ts, tests/cache-ttl.test.ts</files>
  <action>Add tests validating discovery TTL bounds stay within 300-900 seconds and that degraded-state envelope fields are set correctly for success, stale fallback, and upstream-unavailable scenarios.</action>
  <verify>npm run test -- tests/search-degraded-state.test.ts tests/cache-ttl.test.ts</verify>
  <done>Test suite blocks regressions in cache duration policy and degraded-state truthfulness.</done>
</task>

</tasks>

<verification>
Run degraded-state and cache-ttl tests to confirm truthful fallback behavior and policy-compliant cache settings.
</verification>

<success_criteria>
DISC-05 is met with explicit degraded handling and cache-backed supplier responses in the required TTL window.
</success_criteria>

<output>
After completion, create `.planning/phases/02-search-and-discovery-experience/02-03-SUMMARY.md`
</output>
