---
phase: 02-search-and-discovery-experience
plan: 02
subsystem: search
tags: [nextjs, seo, app-router, sitemap, vitest]
requires:
  - phase: 02-search-and-discovery-experience
    provides: Canonical discovery query parsing/serialization from 02-01
provides:
  - SSR destination route at /stays/[destination] with loading and not-found boundaries
  - Destination-specific metadata and canonical URL helpers
  - Legacy /search canonical redirect and sitemap destination route indexing
  - Route-level regression coverage for canonical and crawlability behavior
affects: [search, discovery, seo, routing]
tech-stack:
  added: []
  patterns: [Destination slug allowlist for canonical routing, Metadata helper-driven canonicalization]
key-files:
  created:
    - src/app/stays/[destination]/page.tsx
    - src/app/stays/[destination]/loading.tsx
    - src/app/stays/[destination]/not-found.tsx
    - src/features/search/lib/destination-seo.ts
    - tests/stays-route.test.ts
  modified:
    - src/app/search/page.tsx
    - src/app/sitemap.ts
    - tests/sitemap.test.ts
key-decisions:
  - "Use a shared destination slug allowlist to enforce canonical destination routing and metadata consistency."
  - "Redirect legacy /search requests to /stays/{destination} while preserving normalized discovery query params."
patterns-established:
  - "Destination SEO metadata is generated from a shared helper, not route-local string assembly."
  - "Canonical destination pages own crawlable entrypoints; /search acts as compatibility redirect only."
requirements-completed: [DISC-02]
duration: 6 min
completed: 2026-02-23
---

# Phase 2 Plan 2: Crawlable SSR Destination Route Summary

**Canonical destination result pages now render server-side at `/stays/[destination]` with destination metadata, legacy `/search` redirects, and sitemap indexing aligned to crawl-safe URLs.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-02-23T12:06:17.942Z
- **Completed:** 2026-02-23T12:12:44.758Z
- **Tasks:** 3
- **Files modified:** 8

## Accomplishments
- Added a new SSR App Router destination route with server-side slug validation plus route-level loading and not-found boundaries.
- Introduced destination SEO helpers and wired `generateMetadata` canonical tags so each supported destination emits destination-specific metadata.
- Migrated legacy `/search` into a canonical redirect path to `/stays/{destination}` while preserving normalized discovery query params.
- Extended sitemap generation to include destination canonical pages and added route-level regression tests for metadata, redirects, invalid slugs, and server-render-only route behavior.

## Task Commits

Each task was committed atomically:

1. **Task 1: Build SSR destination route with loading and not-found boundaries** - `72384f3` (feat)
2. **Task 2: Add destination-specific metadata and canonical routing** - `fe28f68` (feat)
3. **Task 3: Cover SSR route behavior with route-level tests** - `c7de657` (test)

## Files Created/Modified
- `src/app/stays/[destination]/page.tsx` - SSR destination route with slug validation and metadata integration.
- `src/app/stays/[destination]/loading.tsx` - Route-level loading fallback for slower destination results.
- `src/app/stays/[destination]/not-found.tsx` - Truthful unsupported destination experience.
- `src/features/search/lib/destination-seo.ts` - Shared supported-destination registry, canonical path helpers, and metadata builder.
- `src/app/search/page.tsx` - Legacy route canonical redirect to destination route.
- `src/app/sitemap.ts` - Sitemap entries expanded with canonical destination URLs.
- `tests/sitemap.test.ts` - Sitemap expectations updated for destination indexing.
- `tests/stays-route.test.ts` - Route-level regressions for metadata, redirect behavior, and not-found handling.

## Decisions Made
- Introduced an explicit supported-destination slug allowlist (`bali`, `dubai`, `kyoto`, `tokyo`, `zurich`) to prevent ambiguous canonical route generation.
- Centralized destination metadata generation in `destination-seo.ts` so canonical URL/title/description stay synchronized across route and sitemap flows.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Updated outdated sitemap assertion to match canonical destination indexing**
- **Found during:** Task 2 (destination-specific metadata and canonical routing)
- **Issue:** `tests/sitemap.test.ts` still asserted only three static routes, blocking verification after destination sitemap entries were intentionally added.
- **Fix:** Updated test expectations to include `/stays/{destination}` URLs generated by sitemap.
- **Files modified:** `tests/sitemap.test.ts`
- **Verification:** `npm run test -- tests/sitemap.test.ts tests/stays-route.test.ts`
- **Committed in:** `fe28f68` (part of Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** The fix aligned regression coverage with planned canonical sitemap behavior and did not expand scope.

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- DISC-02 canonical SSR destination routing is complete and regression-covered.
- Ready for `02-03-PLAN.md` cache/degraded-state search envelope work.

## Self-Check: PASSED

- FOUND: `.planning/phases/02-search-and-discovery-experience/02-02-SUMMARY.md`
- FOUND: `72384f3`
- FOUND: `fe28f68`
- FOUND: `c7de657`
