---
phase: 02-search-and-discovery-experience
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/app/stays/[destination]/page.tsx
  - src/app/stays/[destination]/loading.tsx
  - src/app/stays/[destination]/not-found.tsx
  - src/features/search/lib/destination-seo.ts
  - src/app/search/page.tsx
  - src/app/sitemap.ts
  - tests/stays-route.test.ts
autonomous: true
requirements:
  - DISC-02
must_haves:
  truths:
    - "Destination result pages render server-side with crawlable HTML"
    - "Each destination page emits destination-specific metadata and canonical URL"
    - "Invalid destination routes surface a truthful not-found experience"
  artifacts:
    - path: "src/app/stays/[destination]/page.tsx"
      provides: "SSR destination entry route for search results"
    - path: "src/features/search/lib/destination-seo.ts"
      provides: "Metadata and canonical helper logic for destination pages"
    - path: "src/app/stays/[destination]/not-found.tsx"
      provides: "Invalid destination fallback page"
  key_links:
    - from: "src/app/stays/[destination]/page.tsx"
      to: "src/features/search/lib/destination-seo.ts"
      via: "generateMetadata canonical and title generation"
      pattern: "generateMetadata|canonical"
    - from: "src/app/search/page.tsx"
      to: "src/app/stays/[destination]/page.tsx"
      via: "legacy /search route redirect to canonical destination route"
      pattern: "redirect|stays"
---

<objective>
Ship a canonical SSR destination route that search engines and users can reliably crawl and share.

Purpose: satisfy SEO-critical destination rendering before advanced filter/map behavior expands on top.
Output: `/stays/[destination]` server-rendered route with metadata, loading/not-found states, and legacy `/search` canonical redirect behavior.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-search-and-discovery-experience/02-RESEARCH.md
@.planning/phases/02-search-and-discovery-experience/02-01-SUMMARY.md
@src/app/search/page.tsx
@src/app/sitemap.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build SSR destination route with loading and not-found boundaries</name>
  <files>src/app/stays/[destination]/page.tsx, src/app/stays/[destination]/loading.tsx, src/app/stays/[destination]/not-found.tsx</files>
  <action>Create the App Router destination segment as the new server-rendered search entrypoint. Resolve and validate destination slug + query contract on the server before rendering; return notFound for invalid destination states and keep route-level loading fallback for slow supplier reads.</action>
  <verify>npm run build</verify>
  <done>Destination pages render from server route code with deterministic loading and not-found behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add destination-specific metadata and canonical routing</name>
  <files>src/features/search/lib/destination-seo.ts, src/app/stays/[destination]/page.tsx, src/app/search/page.tsx, src/app/sitemap.ts</files>
  <action>Implement metadata helpers to generate destination titles/descriptions/canonical links and wire them into the new route via `generateMetadata`. Update legacy `/search` handling to redirect to canonical destination URLs, and extend sitemap generation with destination route entries rather than generic `/search` indexing.</action>
  <verify>npm run test -- tests/sitemap.test.ts tests/stays-route.test.ts</verify>
  <done>Canonical destination URLs are emitted consistently in metadata, redirects, and sitemap entries.</done>
</task>

<task type="auto">
  <name>Task 3: Cover SSR route behavior with route-level tests</name>
  <files>tests/stays-route.test.ts</files>
  <action>Add route-focused tests for destination slug validation, canonical redirect from `/search`, metadata generation, and not-found behavior for unsupported destination values. Include assertions that route code remains server-rendered and does not rely on client-only hooks.</action>
  <verify>npm run test -- tests/stays-route.test.ts</verify>
  <done>Automated tests fail if destination SSR route crawlability or canonical behavior regresses.</done>
</task>

</tasks>

<verification>
Build the app and run destination route plus sitemap tests to confirm crawlable SSR routing behavior.
</verification>

<success_criteria>
DISC-02 is met with server-rendered, canonicalized, crawl-safe destination result routes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-search-and-discovery-experience/02-02-SUMMARY.md`
</output>
