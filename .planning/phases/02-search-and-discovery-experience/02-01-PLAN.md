---
phase: 02-search-and-discovery-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/search/lib/discovery-query.ts
  - src/features/search/lib/listing-search-params.ts
  - src/features/search/components/hero-search-bar.tsx
  - src/components/home/mood-discovery.tsx
  - src/features/search/stores/search-ui-store.ts
  - tests/discovery-query.test.ts
autonomous: true
requirements:
  - DISC-01
must_haves:
  truths:
    - "Travelers can submit destination, stay dates, guests, and vibe from home search"
    - "Search URLs preserve all discovery inputs for refresh and sharing"
    - "Invalid or partial query params are normalized to safe defaults"
  artifacts:
    - path: "src/features/search/lib/discovery-query.ts"
      provides: "Canonical parse/serialize contract for discovery URL state"
    - path: "src/features/search/components/hero-search-bar.tsx"
      provides: "Home search form wired to canonical URL contract"
    - path: "tests/discovery-query.test.ts"
      provides: "Regression coverage for query normalization and serialization"
  key_links:
    - from: "src/components/home/mood-discovery.tsx"
      to: "src/features/search/stores/search-ui-store.ts"
      via: "selected vibe persisted in shared search UI state"
      pattern: "activeMood|setActiveMood"
    - from: "src/features/search/components/hero-search-bar.tsx"
      to: "src/features/search/lib/discovery-query.ts"
      via: "URLSearchParams generation from normalized query object"
      pattern: "serialize|URLSearchParams"
---

<objective>
Create one canonical query-state contract for discovery inputs and wire the home search flow to it.

Purpose: remove split-brain input handling before SSR destination and filter work begins.
Output: normalized destination/date/guest/vibe parsing + serialized search URLs generated from the home search form.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-search-and-discovery-experience/02-RESEARCH.md
@src/features/search/lib/listing-search-params.ts
@src/features/search/components/hero-search-bar.tsx
@src/components/home/mood-discovery.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build canonical discovery query parser and serializer</name>
  <files>src/features/search/lib/discovery-query.ts, src/features/search/lib/listing-search-params.ts</files>
  <action>Create a single typed query contract for destination, checkin, checkout, guests, rooms, vibe, language, currency, view, sort, and page values. Centralize validation/defaulting and deterministic serialization so all discovery routes share one URL-state source. Remove ad-hoc param picking logic from listing helpers to avoid divergent behavior.</action>
  <verify>npm run test -- tests/discovery-query.test.ts</verify>
  <done>All discovery input state can be parsed and re-serialized through one module with stable output for equivalent inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Wire home search and mood selection into canonical URL state</name>
  <files>src/features/search/components/hero-search-bar.tsx, src/components/home/mood-discovery.tsx, src/features/search/stores/search-ui-store.ts</files>
  <action>Update home search submission so destination, date range, guest count, and optional mood/vibe are captured and pushed through the canonical serializer. Keep existing autocomplete and guest UX intact, but ensure selected mood contributes to the outgoing query instead of staying local-only in UI state.</action>
  <verify>npm run lint && npm run typecheck</verify>
  <done>Home search submit generates a URL that preserves destination, dates, guests, and vibe across refresh/share/back navigation.</done>
</task>

<task type="auto">
  <name>Task 3: Add normalization and edge-case regression coverage</name>
  <files>tests/discovery-query.test.ts</files>
  <action>Add tests covering invalid dates, out-of-range guest counts, empty destination handling, vibe trimming, and stable serialization ordering. Include equivalent-input tests proving the serializer emits deterministic URLs to prevent cache/key drift.</action>
  <verify>npm run test -- tests/discovery-query.test.ts</verify>
  <done>Test suite fails if discovery query normalization or serialization regresses.</done>
</task>

</tasks>

<verification>
Run the discovery query tests and verify lint/typecheck pass after home search wiring changes.
</verification>

<success_criteria>
DISC-01 is met with canonical URL-backed discovery inputs from the home page.
</success_criteria>

<output>
After completion, create `.planning/phases/02-search-and-discovery-experience/02-01-SUMMARY.md`
</output>
