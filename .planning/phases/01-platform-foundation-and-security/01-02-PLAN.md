---
phase: 01-platform-foundation-and-security
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/006_phase1_foundation.sql
  - src/server/booking/repository.ts
  - src/server/analytics-repository.ts
  - src/server/logger.ts
  - src/server/errors.ts
autonomous: true
requirements:
  - ARCH-04
  - ARCH-05
must_haves:
  truths:
    - "Core platform entities persist in canonical tables"
    - "Booking and search critical paths emit structured logs with correlation IDs"
    - "Operational errors are captured consistently for debugging and alerting"
  artifacts:
    - path: "supabase/migrations/006_phase1_foundation.sql"
      provides: "Canonical schema coverage for phase-1 entities"
    - path: "src/server/logger.ts"
      provides: "Structured logging conventions"
    - path: "src/server/errors.ts"
      provides: "Central error taxonomy/handling"
  key_links:
    - from: "src/server/booking/repository.ts"
      to: "supabase/migrations/006_phase1_foundation.sql"
      via: "table writes/reads for canonical booking records"
      pattern: "from\(|insert\("
    - from: "src/server/errors.ts"
      to: "src/server/logger.ts"
      via: "error logging bridge"
      pattern: "logger"
---

<objective>
Establish canonical persistence and observability primitives required by every later phase.

Purpose: ensure data correctness and diagnosability are foundational, not retrofitted.
Output: schema migration + repository/logging/error updates aligned to phase-1 data model and telemetry needs.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation-and-security/01-RESEARCH.md
@supabase/schema.sql
@supabase/migrations/20260217_ota_core.sql
@src/server/logger.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical schema coverage for phase-1 domain entities</name>
  <files>supabase/migrations/006_phase1_foundation.sql</files>
  <action>Create a migration that ensures canonical tables/columns required for `Users`, `Bookings`, `SavedHotels`, `SearchLogs`, `PaymentLogs`, `AdminUsers`, `CommissionTracking`, and `ReviewsCache` exist with stable keys/indexes and timestamps. Prefer additive, backward-compatible SQL changes that preserve existing production data.</action>
  <verify>supabase db reset --local</verify>
  <done>Local schema applies cleanly and exposes all phase-1 canonical entities with queryable keys.</done>
</task>

<task type="auto">
  <name>Task 2: Align repositories with canonical models</name>
  <files>src/server/booking/repository.ts, src/server/analytics-repository.ts</files>
  <action>Update repository code to read/write canonical columns introduced by migration (including commission/search/payment linkage fields) and remove ad-hoc field assumptions. Keep APIs stateless and deterministic so later admin/revenue phases can reuse these read models.</action>
  <verify>npm run test -- tests/server</verify>
  <done>Repository operations succeed against canonical schema and expose stable typed return shapes.</done>
</task>

<task type="auto">
  <name>Task 3: Standardize structured logging and centralized error capture</name>
  <files>src/server/logger.ts, src/server/errors.ts</files>
  <action>Implement or tighten structured log payload conventions (level, event, correlation_id, route/module, user/admin context, supplier context) and central error helpers that preserve safe public messages plus actionable internal metadata. Wire errors to centralized capture path used by critical APIs.</action>
  <verify>npm run test -- tests/observability || npm run lint</verify>
  <done>Critical server paths emit consistent structured events and errors are captured with correlation metadata.</done>
</task>

</tasks>

<verification>
Confirm migration applies, repositories function against new schema, and logs/errors include correlation-friendly structured fields.
</verification>

<success_criteria>
ARCH-04 and ARCH-05 are satisfied with canonical schema ownership plus actionable telemetry/error instrumentation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation-and-security/01-02-SUMMARY.md`
</output>
