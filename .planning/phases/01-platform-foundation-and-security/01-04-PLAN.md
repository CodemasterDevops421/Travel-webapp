---
phase: 01-platform-foundation-and-security
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-03
files_modified:
  - src/middleware.ts
  - src/server/csrf.ts
  - src/server/ratelimit.ts
  - src/server/request.ts
  - next.config.mjs
  - src/app/api/wishlist/route.ts
  - src/app/api/promo/validate/route.ts
  - src/app/api/booking/prebook/route.ts
  - src/app/api/booking/book/route.ts
autonomous: true
requirements:
  - AUTH-04
  - AUTH-05
must_haves:
  truths:
    - "Cross-origin forged mutation requests are blocked"
    - "Production requests include hardened security headers"
    - "Input and supplier response payloads are validated/sanitized before use"
    - "Rate limiting protects sensitive mutation and auth endpoints"
  artifacts:
    - path: "src/server/csrf.ts"
      provides: "CSRF enforcement utilities"
    - path: "src/middleware.ts"
      provides: "Security header and request policy enforcement"
    - path: "src/server/ratelimit.ts"
      provides: "Shared rate-limit policy"
  key_links:
    - from: "src/app/api/booking/book/route.ts"
      to: "src/server/csrf.ts"
      via: "mutation request guard"
      pattern: "assert|origin|csrf"
    - from: "src/middleware.ts"
      to: "next.config.mjs"
      via: "runtime + static header policy alignment"
      pattern: "Content-Security-Policy|X-Frame-Options"
---

<objective>
Finalize phase-1 request hardening so browser and API mutation surfaces are protected by default.

Purpose: reduce exploitability (CSRF/XSS/injection/abuse) before opening later feature phases.
Output: enforced CSRF + rate limits + security headers + payload sanitization across sensitive endpoints.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation-and-security/01-RESEARCH.md
@.planning/phases/01-platform-foundation-and-security/01-01-SUMMARY.md
@.planning/phases/01-platform-foundation-and-security/01-03-SUMMARY.md
@src/server/csrf.ts
@src/middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce CSRF and rate-limit policies on cookie-auth mutations</name>
  <files>src/server/csrf.ts, src/server/ratelimit.ts, src/app/api/wishlist/route.ts, src/app/api/promo/validate/route.ts, src/app/api/booking/prebook/route.ts, src/app/api/booking/book/route.ts</files>
  <action>Apply same-origin/CSRF assertions to cookie-authenticated mutation handlers and wire shared route-class rate limits for auth/booking-sensitive paths. Keep enforcement scoped so safe same-origin flows continue to work while cross-origin and abuse patterns fail closed.</action>
  <verify>npm run test -- tests/security/csrf tests/security/ratelimit</verify>
  <done>Mutation routes consistently reject CSRF-invalid requests and enforce configured rate limits without breaking legitimate flows.</done>
</task>

<task type="auto">
  <name>Task 2: Harden security headers and CSP configuration</name>
  <files>src/middleware.ts, next.config.mjs</files>
  <action>Implement strict security headers and tighten CSP defaults to reduce script-injection risk while preserving required payment/provider integrations. Align middleware/runtime header injection with Next.js config-level headers to avoid policy drift.</action>
  <verify>npm run build</verify>
  <done>Production responses include hardened headers/CSP and app builds successfully with required third-party flows intact.</done>
</task>

<task type="auto">
  <name>Task 3: Validate and sanitize external and user-controlled payloads</name>
  <files>src/server/request.ts, src/app/api/booking/book/route.ts, src/app/api/booking/prebook/route.ts</files>
  <action>Standardize schema validation/sanitization for user input and upstream supplier payload parsing. Reject malformed payloads with safe errors and strip unsafe fields before rendering/storage to reduce injection/XSS risk propagation.</action>
  <verify>npm run test -- tests/security/validation</verify>
  <done>Malformed or unsafe payloads are rejected/sanitized consistently before business logic execution.</done>
</task>

</tasks>

<verification>
Run security-focused tests and perform response-header spot checks on API and page routes.
</verification>

<success_criteria>
AUTH-04 and AUTH-05 are satisfied with enforceable request protection and input/response sanitization across sensitive flows.
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation-and-security/01-04-SUMMARY.md`
</output>
