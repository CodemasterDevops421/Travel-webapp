---
phase: 01-platform-foundation-and-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/env.ts
  - src/server/liteapi.ts
  - src/server/request.ts
  - src/app/api/hotels/rates/route.ts
  - src/app/api/hotels/[hotelId]/route.ts
  - src/app/api/booking/prebook/route.ts
  - src/app/api/booking/book/route.ts
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03
must_haves:
  truths:
    - "Supplier API keys are never returned to browser clients"
    - "Operators can switch sandbox vs production behavior through server-side config"
    - "API handlers stay stateless and reusable for non-web clients"
  artifacts:
    - path: "src/server/env.ts"
      provides: "Validated multi-environment configuration"
    - path: "src/server/liteapi.ts"
      provides: "Server-only LiteAPI client/proxy boundary"
    - path: "src/app/api/booking/book/route.ts"
      provides: "Stateless booking proxy endpoint"
  key_links:
    - from: "src/app/api/booking/book/route.ts"
      to: "src/server/liteapi.ts"
      via: "server-side supplier call"
      pattern: "liteapi"
    - from: "src/server/liteapi.ts"
      to: "src/server/env.ts"
      via: "environment mode/key selection"
      pattern: "SANDBOX|PRODUCTION"
---

<objective>
Lock the server runtime boundary for supplier access and environment control before feature expansion.

Purpose: prevent key leakage and mode drift while establishing stateless API contracts.
Output: hardened LiteAPI server proxy + explicit sandbox/production selection used by booking and hotel endpoints.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation-and-security/01-RESEARCH.md
@src/server/env.ts
@src/server/liteapi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce server-only multi-environment LiteAPI configuration</name>
  <files>src/server/env.ts, src/server/liteapi.ts</files>
  <action>Refactor environment loading so LiteAPI sandbox and production credentials are validated server-side with explicit mode selection and fail-fast checks. Reject startup when required mode credentials are missing in production paths. Keep all supplier key references out of public env modules and browser bundles.</action>
  <verify>npm run lint && npm run typecheck</verify>
  <done>Runtime env module exposes a single typed mode contract, and LiteAPI client initialization fails closed for invalid/missing credentials.</done>
</task>

<task type="auto">
  <name>Task 2: Route supplier access through stateless API handlers</name>
  <files>src/server/request.ts, src/app/api/hotels/rates/route.ts, src/app/api/hotels/[hotelId]/route.ts, src/app/api/booking/prebook/route.ts, src/app/api/booking/book/route.ts</files>
  <action>Update supplier-facing route handlers to use shared server request wrappers and the centralized LiteAPI proxy module. Ensure each route derives all needed context from request payload/session and performs no in-memory stateful coupling so future mobile clients can call the same endpoints.</action>
  <verify>npm run test -- tests/api</verify>
  <done>Hotel and booking routes call LiteAPI only via server proxy utilities and remain stateless across requests.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression checks for key exposure and mode behavior</name>
  <files>src/app/api/hotels/rates/route.ts, src/app/api/booking/book/route.ts</files>
  <action>Add or update endpoint tests to verify responses never include supplier secrets and that sandbox/production mode selection follows server configuration. Include explicit assertions for degraded error behavior when credentials are absent.</action>
  <verify>npm run test -- tests/api/hotels tests/api/booking</verify>
  <done>Automated tests fail if key material appears in API responses or if mode selection falls back unsafely.</done>
</task>

</tasks>

<verification>
Run API route tests and inspect representative responses to confirm no supplier key exposure and deterministic mode selection.
</verification>

<success_criteria>
ARCH-01, ARCH-02, and ARCH-03 are satisfied with server-enforced supplier boundaries and stateless API behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation-and-security/01-01-SUMMARY.md`
</output>
