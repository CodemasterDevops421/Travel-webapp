---
phase: 01-platform-foundation-and-security
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - src/app/auth/signup/page.tsx
  - src/app/auth/login/page.tsx
  - src/app/auth/callback/route.ts
  - src/shared/hooks/use-auth.ts
  - src/server/supabase/server.ts
  - src/server/authz.ts
  - src/app/api/admin/stats/route.ts
  - src/app/admin/page.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
user_setup:
  - service: google-oauth
    why: "Enable sign-in with Google"
    env_vars:
      - name: GOOGLE_CLIENT_ID
        source: "Google Cloud Console -> OAuth 2.0 Client IDs"
      - name: GOOGLE_CLIENT_SECRET
        source: "Google Cloud Console -> OAuth 2.0 Client Secret"
    dashboard_config:
      - task: "Register auth callback URL"
        location: "Google Cloud Console -> Authorized redirect URIs"
must_haves:
  truths:
    - "Users can authenticate with email/password and keep secure session tokens"
    - "Users can authenticate with Google OAuth and map to correct account identity"
    - "Non-admin authenticated users cannot access admin APIs or admin pages"
  artifacts:
    - path: "src/app/auth/login/page.tsx"
      provides: "Credential login experience"
    - path: "src/app/auth/callback/route.ts"
      provides: "OAuth callback/linking behavior"
    - path: "src/server/authz.ts"
      provides: "Admin authorization guard"
  key_links:
    - from: "src/app/auth/callback/route.ts"
      to: "src/server/supabase/server.ts"
      via: "session/account establishment"
      pattern: "supabase"
    - from: "src/app/api/admin/stats/route.ts"
      to: "src/server/authz.ts"
      via: "RBAC guard"
      pattern: "assert|require"
---

<objective>
Implement phase-1 identity and authorization controls for user and admin boundaries.

Purpose: establish trusted authentication flows and strict admin-only access enforcement.
Output: hardened email/password + Google OAuth auth flows and RBAC-protected admin API/UI paths.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation-and-security/01-RESEARCH.md
@.planning/phases/01-platform-foundation-and-security/01-02-SUMMARY.md
@src/server/authz.ts
@src/app/auth/callback/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden email/password and JWT-backed session behavior</name>
  <files>src/app/auth/signup/page.tsx, src/app/auth/login/page.tsx, src/shared/hooks/use-auth.ts, src/server/supabase/server.ts</files>
  <action>Ensure signup/login flows enforce secure credential handling (bcrypt-backed where applicable), issue/refresh JWT-compatible sessions through the existing auth provider contract, and return consistent auth state for protected pages. Add explicit validation/error messaging for invalid credentials without leaking internal details.</action>
  <verify>npm run test -- tests/auth</verify>
  <done>Email/password auth works end-to-end with secure session semantics and predictable failure behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Google OAuth callback and account linking rules</name>
  <files>src/app/auth/callback/route.ts, src/shared/hooks/use-auth.ts</files>
  <action>Update OAuth callback handling so Google-authenticated users map to existing account records by verified email when available, else create new user identity safely. Preserve anti-replay/state checks and avoid duplicate account creation paths.</action>
  <verify>npm run test -- tests/auth/oauth</verify>
  <done>Google OAuth login succeeds with deterministic account-linking behavior and no duplicate user creation on repeated callback flows.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce RBAC on admin API and admin UI access</name>
  <files>src/server/authz.ts, src/app/api/admin/stats/route.ts, src/app/admin/page.tsx</files>
  <action>Implement explicit role/claim verification helpers and require them on admin API handlers and admin page data access paths. Return deny responses for authenticated non-admin users and preserve successful access for admin roles only.</action>
  <verify>npm run test -- tests/admin</verify>
  <done>Admin endpoints/pages are inaccessible to non-admin users and accessible to users with valid admin role claims.</done>
</task>

</tasks>

<verification>
Run auth/admin test suites to verify credential login, OAuth linking, and RBAC boundaries.
</verification>

<success_criteria>
AUTH-01, AUTH-02, and AUTH-03 are satisfied with secure multi-provider authentication and enforced admin authorization.
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation-and-security/01-03-SUMMARY.md`
</output>
