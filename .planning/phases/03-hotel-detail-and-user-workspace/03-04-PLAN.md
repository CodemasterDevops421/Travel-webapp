---
phase: 03-hotel-detail-and-user-workspace
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/app/api/wishlist/route.ts
  - src/shared/hooks/use-wishlist.ts
  - src/features/hotels/components/hotel-detail-experience.tsx
  - src/features/search/components/horizontal-hotel-card.tsx
  - src/app/wishlist/page.tsx
  - tests/wishlist-workspace-flow.test.tsx
autonomous: true
requirements:
  - HOTL-04
must_haves:
  truths:
    - "Authenticated users can save and remove hotels from both detail and search surfaces"
    - "Unauthenticated users get explicit login-to-save guidance instead of silent wishlist failures"
    - "Wishlist workspace shows persisted saved hotels and stays consistent with toggle actions"
  artifacts:
    - path: "src/app/api/wishlist/route.ts"
      provides: "Authenticated wishlist retrieve/save/remove contract with clear error semantics"
    - path: "src/shared/hooks/use-wishlist.ts"
      provides: "Unified client wishlist state and optimistic save/remove behavior"
    - path: "src/app/wishlist/page.tsx"
      provides: "Account workspace retrieval view for saved hotels"
    - path: "tests/wishlist-workspace-flow.test.tsx"
      provides: "Regression coverage for save/remove/retrieve workflow"
  key_links:
    - from: "src/features/hotels/components/hotel-detail-experience.tsx"
      to: "src/app/api/wishlist/route.ts"
      via: "save/remove toggle invokes authenticated wishlist API through shared hook"
      pattern: "toggleSave|wishlist"
    - from: "src/features/search/components/horizontal-hotel-card.tsx"
      to: "src/shared/hooks/use-wishlist.ts"
      via: "search-card save state reflects shared wishlist store"
      pattern: "isSaved|toggleSave"
---

<objective>
Complete the authenticated wishlist-to-workspace loop across hotel and search surfaces with consistent state and clear auth behavior.

Purpose: travelers should be able to save interesting stays during discovery/evaluation and reliably retrieve them in their account workspace.
Output: integrated wishlist toggles, hardened wishlist API/hook semantics, and workspace retrieval regression coverage.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hotel-detail-and-user-workspace/03-RESEARCH.md
@.planning/phases/03-hotel-detail-and-user-workspace/03-02-SUMMARY.md
@src/app/api/wishlist/route.ts
@src/shared/hooks/use-wishlist.ts
@src/features/hotels/components/hotel-detail-experience.tsx
@src/features/search/components/horizontal-hotel-card.tsx
@src/app/wishlist/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden wishlist API and shared hook auth/error semantics</name>
  <files>src/app/api/wishlist/route.ts, src/shared/hooks/use-wishlist.ts</files>
  <action>Standardize wishlist route responses and hook behavior for authenticated, unauthorized, and transient-error paths. Keep CSRF/rate-limit protections intact, preserve optimistic UX with reliable rollback, and expose enough state for UI surfaces to show explicit auth-required messaging.</action>
  <verify>npm run lint && npm run typecheck</verify>
  <done>Wishlist API/hook contract handles save/remove/retrieve and unauthorized cases consistently without silent failures.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate save/remove actions on hotel detail and search cards</name>
  <files>src/features/hotels/components/hotel-detail-experience.tsx, src/features/search/components/horizontal-hotel-card.tsx</files>
  <action>Wire a shared wishlist toggle interaction into hotel detail and search card surfaces so both reflect current saved state and support add/remove actions. For anonymous users, show explicit login redirect guidance rather than no-op actions.</action>
  <verify>npm run test -- tests/search-mobile-responsive.test.tsx</verify>
  <done>Both detail and search surfaces can save/remove hotels with visible auth-aware behavior and synchronized state.</done>
</task>

<task type="auto">
  <name>Task 3: Refine wishlist page as account workspace retrieval surface with regressions</name>
  <files>src/app/wishlist/page.tsx, tests/wishlist-workspace-flow.test.tsx</files>
  <action>Upgrade wishlist page UX to emphasize account workspace retrieval (saved list, empty state, remove actions, refresh consistency) and add end-to-end style regression tests for save/remove/retrieve lifecycle from multiple entry surfaces.</action>
  <verify>npm run test -- tests/wishlist-workspace-flow.test.tsx</verify>
  <done>Account workspace reliably reflects persisted wishlist state and is protected by automated save/remove/retrieve regression coverage.</done>
</task>

</tasks>

<verification>
Run HOTL-04 workflow tests and confirm authenticated/unauthenticated wishlist behavior across search, detail, and workspace surfaces.
</verification>

<success_criteria>
HOTL-04 is met with authenticated wishlist persistence, cross-surface save/remove controls, and reliable workspace retrieval.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hotel-detail-and-user-workspace/03-04-SUMMARY.md`
</output>
