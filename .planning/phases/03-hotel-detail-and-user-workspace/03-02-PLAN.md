---
phase: 03-hotel-detail-and-user-workspace
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/app/api/hotels/rates/route.ts
  - src/features/hotels/hooks/use-hotel-rates.ts
  - src/features/hotels/components/hotel-detail-experience.tsx
  - src/app/booking/page.tsx
  - tests/hotel-booking-card.test.tsx
autonomous: true
requirements:
  - HOTL-02
must_haves:
  truths:
    - "Travelers can select a room and see the same selected rate reflected in the sticky booking card"
    - "Cancellation context is visible before booking hand-off and tied to the selected rate"
    - "Booking CTA forwards a complete, consistent room selection payload into checkout"
  artifacts:
    - path: "src/features/hotels/components/hotel-detail-experience.tsx"
      provides: "Canonical selected-room state and sticky booking card bound to selected rate"
    - path: "src/app/api/hotels/rates/route.ts"
      provides: "Rates payload exposing cancellation-context fields for UI display"
    - path: "tests/hotel-booking-card.test.tsx"
      provides: "Regression checks for room selection, sticky card sync, and cancellation visibility"
  key_links:
    - from: "src/features/hotels/components/hotel-detail-experience.tsx"
      to: "src/app/api/hotels/rates/route.ts"
      via: "selected rate consumes cancellation metadata from rates API"
      pattern: "cancel|refund|selected"
    - from: "src/features/hotels/components/hotel-detail-experience.tsx"
      to: "src/app/booking/page.tsx"
      via: "booking query hand-off carries selected room and pricing context"
      pattern: "hotelId|roomId|offerId|amount|checkIn|checkOut"
---

<objective>
Bind room selection, sticky booking card content, and cancellation messaging into one consistent booking-context flow.

Purpose: travelers must understand what they are booking and cancellation implications before moving into checkout.
Output: a selected-rate contract that drives room cards, sticky booking sidebar, and booking query hand-off with regression coverage.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hotel-detail-and-user-workspace/03-RESEARCH.md
@.planning/phases/03-hotel-detail-and-user-workspace/03-01-SUMMARY.md
@src/app/api/hotels/rates/route.ts
@src/features/hotels/hooks/use-hotel-rates.ts
@src/features/hotels/components/hotel-detail-experience.tsx
@src/app/booking/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize cancellation policy context in rates API contract</name>
  <files>src/app/api/hotels/rates/route.ts, src/features/hotels/hooks/use-hotel-rates.ts</files>
  <action>Audit current supplier rate payload handling and expose stable cancellation-context fields (for example refundable status, cancel deadline, and cancellation notes) through the rates API and client hook types. Keep these fields explicit and nullable rather than inferred from UI strings.</action>
  <verify>npm run lint && npm run typecheck</verify>
  <done>Rates API/hook deliver cancellation-context fields needed for visible, rate-specific cancellation messaging.</done>
</task>

<task type="auto">
  <name>Task 2: Implement selected-room source of truth for room cards and sticky booking card</name>
  <files>src/features/hotels/components/hotel-detail-experience.tsx</files>
  <action>Introduce one selected-rate state model and derive room highlight, sticky card summary, cancellation copy, and booking CTA query payload from it. Avoid duplicate calculations between room rows and sidebar to prevent state drift.</action>
  <verify>npm run test -- tests/booking-page-auth.test.ts</verify>
  <done>Room selection, sticky card details, and booking CTA always reflect the same selected offer and cancellation context.</done>
</task>

<task type="auto">
  <name>Task 3: Add HOTL-02 regression tests for selection and cancellation visibility</name>
  <files>src/app/booking/page.tsx, tests/hotel-booking-card.test.tsx</files>
  <action>Add tests covering default selected-room behavior, user room switching, sticky sidebar sync, and booking query hand-off including cancellation fields. Ensure tests fail if cancellation context becomes hidden or disconnected from selected room state.</action>
  <verify>npm run test -- tests/hotel-booking-card.test.tsx</verify>
  <done>Regression tests protect sticky-card/room-selection integrity and visible cancellation policy context before checkout hand-off.</done>
</task>

</tasks>

<verification>
Run type checks and HOTL-02 regression tests to verify selected-room synchronization and cancellation visibility guarantees.
</verification>

<success_criteria>
HOTL-02 is met with a sticky booking card tied to selected room state and visible cancellation policy context carried into booking hand-off.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hotel-detail-and-user-workspace/03-02-SUMMARY.md`
</output>
