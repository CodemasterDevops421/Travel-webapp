---
phase: 03-hotel-detail-and-user-workspace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/liteapi.ts
  - src/app/api/hotels/[hotelId]/route.ts
  - src/app/hotels/[hotelId]/page.tsx
  - src/features/hotels/components/hotel-detail-experience.tsx
  - tests/hotel-detail-content.test.ts
autonomous: true
requirements:
  - HOTL-01
must_haves:
  truths:
    - "Hotel pages show supplier-backed content blocks for amenities, gallery, policies, location, reviews, and pros/cons"
    - "When supplier fields are missing, the page explicitly shows truthful partial-data messaging instead of implied completeness"
    - "Hotel detail API and SSR payloads expose one stable contract for all required detail sections"
  artifacts:
    - path: "src/server/liteapi.ts"
      provides: "Normalized hotel detail contract including policies, location context, reviews, and pros/cons fields"
    - path: "src/features/hotels/components/hotel-detail-experience.tsx"
      provides: "Requirement-complete hotel detail rendering with explicit empty/partial states"
    - path: "tests/hotel-detail-content.test.ts"
      provides: "Regression coverage for required content sections and truthful fallbacks"
  key_links:
    - from: "src/server/liteapi.ts"
      to: "src/app/api/hotels/[hotelId]/route.ts"
      via: "normalized supplier payload returned through validated API boundary"
      pattern: "polic|pros|cons|review|location"
    - from: "src/app/hotels/[hotelId]/page.tsx"
      to: "src/features/hotels/components/hotel-detail-experience.tsx"
      via: "SSR detail payload passed into client experience"
      pattern: "HotelDetailExperience"
---

<objective>
Make hotel detail pages requirement-complete and truthful by formalizing one normalized content contract across supplier mapping, API boundaries, and UI sections.

Purpose: travelers need confidence that what they see about a property is complete when available and clearly partial when data is missing.
Output: a contract-driven hotel detail experience that consistently renders amenities, gallery, policies, location context, reviews, and pros/cons with regression coverage.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hotel-detail-and-user-workspace/03-RESEARCH.md
@src/server/liteapi.ts
@src/app/api/hotels/[hotelId]/route.ts
@src/app/hotels/[hotelId]/page.tsx
@src/features/hotels/components/hotel-detail-experience.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand normalized hotel detail contract for HOTL-01 sections</name>
  <files>src/server/liteapi.ts</files>
  <action>Extend the `HotelDetails` normalization shape to explicitly model policy content, location context snippets, review/pros-cons fields, and nullability semantics for partial supplier payloads. Keep mapping centralized in `liteapi.ts` and avoid component-level parsing drift.</action>
  <verify>npm run lint && npm run typecheck</verify>
  <done>The canonical `HotelDetails` contract covers all HOTL-01 content blocks with explicit optional/null handling for missing supplier data.</done>
</task>

<task type="auto">
  <name>Task 2: Thread the enriched detail contract through API and SSR boundaries</name>
  <files>src/app/api/hotels/[hotelId]/route.ts, src/app/hotels/[hotelId]/page.tsx</files>
  <action>Update hotel detail route and page wiring so enriched normalized fields are preserved across API and SSR rendering paths, including safe response behavior for not-found and degraded detail payloads. Keep response changes additive to reduce downstream break risk.</action>
  <verify>npm run test -- tests/stays-route.test.ts</verify>
  <done>Hotel detail API/SSR paths deliver the same enriched contract used by the hotel page UI.</done>
</task>

<task type="auto">
  <name>Task 3: Render requirement-complete sections with truthful fallback messaging and tests</name>
  <files>src/features/hotels/components/hotel-detail-experience.tsx, tests/hotel-detail-content.test.ts</files>
  <action>Update hotel detail sections to render amenities/gallery/policies/location/reviews/pros-cons from normalized data and provide explicit user-facing fallback copy for each missing block. Add regression tests that assert section presence and partial-data truthfulness behavior.</action>
  <verify>npm run test -- tests/hotel-detail-content.test.ts</verify>
  <done>Travelers see complete section coverage where data exists and clear partial-data messaging where supplier data is unavailable.</done>
</task>

</tasks>

<verification>
Run lint/typecheck plus hotel detail regression tests to verify normalized contract completeness and truthful section rendering.
</verification>

<success_criteria>
HOTL-01 is met with supplier-backed hotel detail completeness and explicit partial-state truthfulness across API, SSR, and UI layers.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hotel-detail-and-user-workspace/03-01-SUMMARY.md`
</output>
