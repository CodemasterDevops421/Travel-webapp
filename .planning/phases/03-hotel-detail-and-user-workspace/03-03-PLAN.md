---
phase: 03-hotel-detail-and-user-workspace
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/app/api/hotel-ai/route.ts
  - src/server/hotel-ai-context.ts
  - src/server/concierge.ts
  - tests/hotel-ai-grounding.test.ts
autonomous: true
requirements:
  - HOTL-03
must_haves:
  truths:
    - "Hotel AI answers are grounded in current hotel data instead of generic unsupported claims"
    - "When requested facts are unavailable, AI responds with explicit 'not available in current data' guidance"
    - "Hotel AI never bypasses booking logic or provides payment/confirmation promises"
  artifacts:
    - path: "src/app/api/hotel-ai/route.ts"
      provides: "Validated hotel-AI endpoint with grounding and booking-safety guardrails"
    - path: "src/server/hotel-ai-context.ts"
      provides: "Reusable hotel context extractor for deterministic/model-assisted response generation"
    - path: "tests/hotel-ai-grounding.test.ts"
      provides: "Regression tests for grounding, missing-data handling, and booking-safe boundaries"
  key_links:
    - from: "src/app/api/hotel-ai/route.ts"
      to: "src/server/hotel-ai-context.ts"
      via: "question answering uses normalized hotel-context payload only"
      pattern: "context|ground|hotel"
    - from: "src/app/api/hotel-ai/route.ts"
      to: "src/server/liteapi.ts"
      via: "hotel details fetch supplies allowed answer facts"
      pattern: "getHotelDetails"
---

<objective>
Upgrade hotel-page AI Q&A into a grounded, booking-safe assistant that answers from hotel facts and gracefully handles unknowns.

Purpose: travelers should trust AI answers on hotel pages without receiving speculative claims or booking-logic bypass guidance.
Output: stricter hotel AI request/response contract with grounded context extraction and guardrail regression tests.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hotel-detail-and-user-workspace/03-RESEARCH.md
@.planning/phases/03-hotel-detail-and-user-workspace/03-01-SUMMARY.md
@src/app/api/hotel-ai/route.ts
@src/server/concierge.ts
@src/server/liteapi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define strict hotel-AI grounding contract and response boundaries</name>
  <files>src/app/api/hotel-ai/route.ts</files>
  <action>Refine request validation and answer generation contract so responses are sourced only from current normalized hotel data fields. Enforce explicit unknown-data responses when required facts are absent and forbid payment, booking-confirmation, or policy promises beyond available data.</action>
  <verify>npm run lint && npm run typecheck</verify>
  <done>Hotel AI route consistently returns grounded answers with explicit unknown handling and booking-safe language boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Add reusable hotel-context extraction for deterministic/model-assisted answering</name>
  <files>src/server/hotel-ai-context.ts, src/server/concierge.ts</files>
  <action>Create a focused server utility that shapes allowed hotel facts (amenities, location, policies, reviews/pros-cons, room/cancellation context summary) for AI answering. Reuse existing concierge schema patterns where helpful, while keeping hotel AI scoped to per-hotel context instead of global chat behavior.</action>
  <verify>npm run test -- tests/utils.test.ts</verify>
  <done>Hotel AI answer generation uses one reusable, schema-bounded context payload that prevents ungrounded data access.</done>
</task>

<task type="auto">
  <name>Task 3: Add HOTL-03 regression tests for grounded and booking-safe answers</name>
  <files>tests/hotel-ai-grounding.test.ts</files>
  <action>Add tests for core intents (amenities, location, cancellation/policy, unavailable facts) and negative-path assertions that booking/payment promises are rejected or redirected to booking terms. Include checks that answers do not invent fields absent from hotel payload.</action>
  <verify>npm run test -- tests/hotel-ai-grounding.test.ts</verify>
  <done>Automated coverage prevents regressions that would allow hallucinated or booking-unsafe AI responses.</done>
</task>

</tasks>

<verification>
Run hotel AI grounding tests plus lint/typecheck to confirm scoped, hotel-data-grounded, booking-safe Q&A behavior.
</verification>

<success_criteria>
HOTL-03 is met with contextual hotel AI responses that stay within available hotel data and booking-safe boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hotel-detail-and-user-workspace/03-03-SUMMARY.md`
</output>
