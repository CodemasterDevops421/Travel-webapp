name: ci

on:
  pull_request:
  push:
    branches: [main, master]

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: Blog SEO gate
        run: npm run blog:seo:gate

      - name: Weekly KPI report contract
        run: npm run blog:kpi:weekly

      - name: CMS parity contract
        run: npm run cms:parity

      - name: Blog scale engine checks
        run: npm run blog:scale:verify

      - name: Conversion attribution and experiment checks
        run: npm run blog:conversion:verify

      - name: Build
        run: npm run build

      - name: Audit runtime deps
        run: npm audit --omit=dev
